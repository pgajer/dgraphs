% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isometry_deviation.R
\name{isometry.geodesic.diagnostics}
\alias{isometry.geodesic.diagnostics}
\title{Compute Geodesic-Isometry Diagnostics}
\usage{
isometry.geodesic.diagnostics(
  D.estimated,
  D.true,
  scale = TRUE,
  true.tol = sqrt(.Machine$double.eps),
  band.probs = c(1/3, 2/3)
)
}
\arguments{
\item{D.estimated}{Estimated distance matrix or \code{dist} object.}

\item{D.true}{Ground-truth distance matrix or \code{dist} object.}

\item{scale}{Logical scalar. If \code{TRUE}, use \code{isometry.scale()} to optimally
rescale estimated distances before computing the error.}

\item{true.tol}{Non-negative tolerance. Pairs with true distance less than
or equal to this value are excluded from relative metrics.}

\item{band.probs}{Numeric vector of two probabilities used to split
reference distances into short, middle, and long distance bands.}
}
\value{
Named numeric vector with target-normalized graph-geodesic relative
RMSE, signed residual bias, shortcut fraction, relative absolute residual
quantiles, and median signed relative residuals in short/mid/long
reference-distance bands. The relative-RMSE entry retains the historical
name \code{rel_geodesic_stress}; it is not Kruskal's Stress-1.
}
\description{
Computes signed and scale-regime diagnostics for comparing estimated graph
geodesic distances with reference geodesic distances. Distances are first
optionally calibrated by the least-squares scale from \code{isometry.scale()}.
}
\examples{
D <- as.matrix(dist(1:4))
isometry.geodesic.diagnostics(1.1 * D, D)

}
