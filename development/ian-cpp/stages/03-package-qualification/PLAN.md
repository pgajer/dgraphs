# Stage 3: fresh internal package qualification on macOS arm64

23 September 2026. Stages 1 and 2 are independently accepted with all findings closed. The owner authorized progression through the five-stage sequence. The initial supported internal target is macOS arm64; the optional platform preference was not answered, so the stated Mac-first recommendation applies. Linux, Windows and Intel Mac remain unqualified and no remote infrastructure is purchased.

## Scope and prospective changes

Qualify the available R-devel runtime and R 4.5.2 on this host, using separate fresh private package libraries and fresh optional backend builds from installed sources. Preserve strict default and explicit retry-power option without numerical changes. Keep the R 500-row guard and no-restart boundary; this stage does not expand the API or export the function. Update stale audit/adoption wording in source documentation and regenerate help. Exclude coordinator development/experiment files from package distribution, while retaining required source and license notices. Inspect and correct IAN packaging/build defects discovered by actual checks. Retain every failed attempt and distinguish unrelated existing package limitations from IAN regressions.

Use the existing Makefile document/build/check targets where applicable. Build a source archive with recorded provenance; install into fresh libraries. Verify package archive contents and all installed backend source identities, accounting explicitly for normal R exclusions. Run the installed helper with fresh build directories, pinned native-arm Rust 1.85.1 and the locked dependency graph. Use cached crates offline. R 4.5.2 gets isolated library variables and a private launcher selecting its actual runtime, avoiding the previously documented framework redirection. Existing compatible dependency installations may be reused with paths/versions recorded; no claim to have rebuilt every dependency. Backend/module construction is fresh in each runtime.

## Numerical and interface schedule

Each runtime executes the established 13-case adapter panel (four small strict references, duplicate/distance/full-diagnostic controls and numerical/budget/post-graph/interrupt/duplicate refusals). Execute four strict full traces and five explicit retry-power full traces (four small examples plus the frozen 500-profile helix), comparing with accepted Stage-1 native/R results and checking every attempt, settings, graphs, scales and affinities. Add three candidate-policy controls: invalid solver output, solve budget and interruption. Test relocated module loading in one fresh small example per runtime. This is 26 engine entries per runtime, 52 planned in total. All actual calls and solver attempts, including failures, are recorded. Invalid argument/policy/unavailable-backend checks run without invoking the core.

Run ordinary package tests and full package checks on actual available named runtimes. Package IAN tests must remain no-solve tests; any unforeseen IAN execution is counted rather than omitted. Test that ordinary installation works without the optional module and gives the documented backend-unavailable error. Verify no export was added and module dependencies contain no build-directory or private worker-library dependency. Preserve initial/final graph distinction and structured incomplete results. No new Python trajectory solves are planned; Python warning results from Stage 2 are reused and documented.

## Limits and advancement

At most 64 IAN engine entries, 2,000 physical solver attempts cumulatively, 250 per entry, 900 seconds per numerical process and 3,600 aggregate numerical child seconds; sampled process-tree RSS 4 GiB, 2 GiB output per numerical process, 24 GiB total numerical evidence and at least 20 GiB free. Build/package checks are separate recorded commands, at most two hours per command and eight aggregate hours; two parallel compiler jobs maximum. Do not interpret compilation as runtime qualification. A numerical discrepancy stops dependent calls and requires a declared bounded correction study; resource refusal is retained and does not silently increase limits.

Completion requires fresh install/build behavior, declared interface/numerical/failure comparisons, explicit disposition of package checks, reproducible dependency/licensing records and independent acceptance with all findings closed. The claim is bounded internal support for actual tested Mac/R combinations, not CRAN or public-release readiness. Missing platforms stay on the future list. Source plans and scripts are maintained here; evidence goes under the dated private stage03-package area. Record a concise completion report in Project status and next steps after audit; then Stage 4 may begin.
