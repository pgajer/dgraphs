% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodesic_stats.R
\name{plot.geodesic_stats}
\alias{plot.geodesic_stats}
\title{Plot Geodesic Statistics}
\usage{
\method{plot}{geodesic_stats}(
  x,
  plot.type = c("summary", "heatmap", "vertex", "all"),
  selected.vertices = NULL,
  max.vertices = 10,
  ...
)
}
\arguments{
\item{x}{List. Output from compute.geodesic.stats().}

\item{plot.type}{Character. Type of plot to generate:
"summary" (default), "heatmap", "vertex", or "all".}

\item{selected.vertices}{Integer vector. Specific vertices to highlight
in "vertex" plots. If NULL, a random sample will be used.}

\item{max.vertices}{Integer. Maximum number of vertices to show in vertex plot.}

\item{...}{Additional arguments passed to summary}
}
\value{
Invisibly returns NULL.
}
\description{
Creates visualizations of geodesic statistics to help understand
the relationship between radius and number of geodesics.
}
\examples{
stats <- list(
  radii = c(0.2, 0.4),
  geodesic_rays = matrix(c(2, 3, 3, 4), nrow = 2, byrow = TRUE),
  composite_geodesics = matrix(c(1, 1, 1, 2), nrow = 2, byrow = TRUE),
  grid_vertices = c(5L, 9L),
  summary = data.frame(
    radius = c(0.2, 0.4),
    avg_rays = c(2.5, 3.5),
    max_rays = c(3, 4),
    composite_ratio = c(0.4, 0.5),
    avg_overlap_median = c(0.35, 0.45)
  )
)
class(stats) <- c("geodesic_stats", "list")

plot(stats, plot.type = "summary")
plot(stats, plot.type = "vertex", selected.vertices = 5)

}
