% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_edit_distance.R
\name{create.distance.plot}
\alias{create.distance.plot}
\title{Create Distance Plot}
\usage{
create.distance.plot(
  x,
  y,
  smooth = TRUE,
  smooth.method = "loess",
  mark.x = NULL,
  xlab = "Number of Nearest Neighbors (k)",
  ylab = "Edit Distance",
  main = NULL,
  width = 6,
  height = 6,
  ...
)
}
\arguments{
\item{x}{Numeric vector of x-axis values (e.g., k values).}

\item{y}{Numeric vector of y-axis values (e.g., edit distances).}

\item{smooth}{Logical indicating whether to add a smoothing line
(default: TRUE).}

\item{smooth.method}{Character string specifying the smoothing method.
Options include "loess" (default), "lm", "glm", "gam".}

\item{mark.x}{Optional numeric value(s) to mark with vertical lines.}

\item{xlab}{Character string for x-axis label
(default: "Number of Nearest Neighbors (k)").}

\item{ylab}{Character string for y-axis label (default: "Edit Distance").}

\item{main}{Character string for plot title (default: NULL, no title).}

\item{width}{Numeric value for plot width in inches (default: 6).}

\item{height}{Numeric value for plot height in inches (default: 6).}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Returns \code{NULL} invisibly after drawing the plot.
}
\description{
Creates a plot of edit distances with optional smoothing or
regression results.
}
\details{
The function creates a scatter plot with optional smoothing line. If
\code{mark.x} is provided, vertical dashed lines are added at those x-values.
}
\examples{
k.vals <- c(5, 10, 15, 20)
distances <- c(2.2, 1.4, 1.1, 1.3)
create.distance.plot(k.vals, distances, smooth = FALSE)

min.k <- k.vals[which.min(distances)]
create.distance.plot(k.vals, distances, mark.x = min.k,
                     smooth = FALSE,
                     main = "Edit Distance vs k")
}
