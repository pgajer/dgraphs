# R integer-sentinel correction and repeated qualification

23 September 2026. All first-panel 28 native/R entries completed and matched. An additional author projection probe, using the actual bridge template, showed that INT_MIN was converted to R's integer NA sentinel. This boundary was absent from ordinary numerical outputs. Preserve the first build, full panel, and failed probe. Correct the conversion to use an exactly representable R double for INT_MIN; add a direct R projection test rather than relying only on a C++ classifier expectation.

Freshly snapshot/build/install the corrected candidate and repeat all 28 main entries. Keep the original numerical rules, fixtures, thresholds, per-process/time/memory/output and aggregate 16,000-attempt/7,200-second budgets. Increase only the prospective cumulative engine-entry allowance from 64 to 80 to accommodate the full repeated panel (74 entries if every originally planned supplemental and conditional size test executes). This amendment is committed before corrected measurements. The first 28 entries count against the revised total; no evidence is overwritten. Package checks and dimension/projection-only controls remain separate and invoke no solver. This is an implementation correction under the owner's authorization, not an adjustment to obtain numerical acceptance.
