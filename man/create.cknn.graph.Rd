% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radius_graphs.R
\name{create.cknn.graph}
\alias{create.cknn.graph}
\title{Compute a Continuous-kNN Graph}
\usage{
create.cknn.graph(
  X,
  k.scale,
  delta = 1,
  radius.search = c("ann", "all.pairs"),
  return.timing = FALSE,
  graph.detail = c("full", "minimal"),
  prune.method = c("none", "local.geodesic", "global.geodesic.ratio"),
  max.path.edge.ratio.deviation.thld = 0.1,
  path.edge.ratio.percentile = 0.5,
  prune.tau = 1.05,
  prune.local.k = NULL,
  with.pruned.edge.stats = FALSE,
  connect.components = FALSE,
  connect.method = c("component.mst", "component.mst.ann", "global.mst"),
  bridge.k = NULL,
  bridge.k.max = NULL,
  bridge.growth = 2
)
}
\arguments{
\item{X}{Numeric matrix or data frame with observations in rows.}

\item{k.scale}{Positive integer smaller than \code{nrow(X)}. Defines local scale
distances \eqn{\sigma_i}.}

\item{delta}{Positive numeric scalar multiplying the geometric-mean adaptive
radius.}

\item{radius.search}{Character scalar. \code{"ann"} uses the bundled ANN kd-tree
exact radius-search backend inherited from \code{create.rknn.graph()}.
\code{"all.pairs"} uses the direct \eqn{O(n^2)} reference path.}

\item{return.timing}{Logical scalar. If \code{TRUE}, attach the same construction
timing table returned by \code{create.rknn.graph()}, including ANN
setup, local-scale search, fixed-radius candidate search, edge
materialization, and graph finalization when \code{radius.search = "ann"}.}

\item{graph.detail}{Character scalar. \code{"full"} returns the complete graph
lifecycle object. \code{"minimal"} returns only core graph fields and is allowed
only with \code{prune.method = "none"} and \code{connect.components = FALSE}.}

\item{prune.method}{Character scalar. \code{"none"} disables geometric pruning.
\code{"local.geodesic"} applies the experimental local geometric pruning stage
before optional MST connectivity repair. \code{"global.geodesic.ratio"} applies
whole-graph geodesic-ratio pruning before optional MST connectivity repair.}

\item{max.path.edge.ratio.deviation.thld}{Numeric scalar in \verb{[0, 0.2)}.
For \code{prune.method = "global.geodesic.ratio"}, an edge may be removed when
the shortest alternative path is at most
\code{1 + max.path.edge.ratio.deviation.thld} times the direct edge length.}

\item{path.edge.ratio.percentile}{Numeric scalar in \verb{[0, 1]}. For
\code{prune.method = "global.geodesic.ratio"}, only edges at or above this edge
length percentile are considered.}

\item{prune.tau}{Numeric scalar greater than 1. For local geometric pruning,
an edge may be removed when a retained local alternative path is at most
this multiplicative factor times the direct edge length.}

\item{prune.local.k}{Integer scalar or \code{NULL}. Number of nearest neighbors
used to form local neighborhoods for pruning. For fixed-radius graphs,
\code{NULL} uses the median positive raw graph degree, clipped to \verb{[1, n - 1]}.
For adaptive-radius graphs, \code{NULL} defaults to \code{k.scale}.}

\item{with.pruned.edge.stats}{Logical scalar. If \code{TRUE}, return a data frame
with one row per locally pruned edge.}

\item{connect.components}{Logical scalar. If \code{TRUE}, add MST bridge edges so
the final graph is connected whenever possible.}

\item{connect.method}{Character scalar. \code{"component.mst"} adds exact shortest
inter-component bridges. \code{"component.mst.ann"} tries sparse ANN bridge
candidates before automatic exact fallback. \code{"global.mst"} unions the graph
with the full Euclidean MST.}

\item{bridge.k}{Integer scalar or \code{NULL}. Initial ANN bridge neighborhood
size for \code{connect.method = "component.mst.ann"}.}

\item{bridge.k.max}{Integer scalar or \code{NULL}. Maximum ANN bridge neighborhood
size before exact fallback.}

\item{bridge.growth}{Numeric scalar greater than 1. Multiplicative growth
factor for ANN bridge neighborhoods.}
}
\value{
A list inheriting from \code{"cknn_graph"} and \code{"adaptive_radius_graph"}.
It contains the same lifecycle fields as \code{create.rknn.graph()} with
\code{type = "adaptive.radius"}, \code{radius_rule = "geomean"},
\code{radius_factor = delta}, and \code{graph_rule = "continuous.knn"}.
}
\description{
Creates an undirected continuous-kNN graph using local scale distances.
Let \eqn{\sigma_i} be the distance from observation \eqn{i} to its
\code{k.scale}-th non-self nearest neighbor. Vertices are adjacent when
\deqn{\|x_i - x_j\|_2 \le \delta \sqrt{\sigma_i\sigma_j}.}

This is a convenience wrapper around \code{create.rknn.graph()} with
\code{type = "adaptive.radius"} and \code{radius.rule = "geomean"}. It is useful as
a named data-derived graph construction in geodesic reconstruction
benchmarks.
}
\examples{
X <- matrix(c(0, 1, 3), ncol = 1)
create.cknn.graph(X, k.scale = 1, delta = 1)$edge_matrix

}
