# Stage 2: frozen 1,000-profile scale panel

23 September 2026. Stage 1 is independently accepted, all findings closed, and its explicit retry-power option has been selected for internal qualification. This prospective study resumes the six unexecuted inputs of Phase07F without resampling. It is the next step of the owner's authorized sequence.

## Inputs and numerical rules

Reuse the accepted Stage-1 native binary and corrected Python reference, hash-verified before execution. No numerical source, dependency, acceptance threshold or default changes. Select `IAN evaluated-LP retry-power 0.1` explicitly in copies of the frozen Phase07F inputs; every other parsed field must equal the original. Execute native then Python in this order: noisy six-dimensional cloud, separated planar lobes, quadforms of intrinsic dimension 2, 3, 4 and 5 (ambient 3, 4, 5 and 6). The quadforms retain the audited latent-uniform sampling, fixed quadratic forms and original seeds. Their scientific quality is not being scored here.

The accepted Stage-1 1,000-profile helix and 500-profile panel are reused prerequisites, labeled as earlier executions. Do not repeat them. Keep frozen geometry/provenance and compare complete trajectories, settings, rejection/retry histories, original-unit certificates, initial/final graphs, scales and affinities. Native checkpoint interval remains 100; verify saved envelopes and trace-prefix bindings. The Stage-1 cancellation/resume test remains reused operational evidence; no new recovery solves are needed for an unchanged core.

## Accounting and stop rules

At most 12 principal engine launches, serial, one numerical thread. Up to 1,000 physical attempts per child and 10,000 cumulatively, 900 seconds per child and 3,600 cumulative child seconds, 4 GiB sampled process-tree RSS, 2 GiB per-call output and 24 GiB stage output, at least 20 GiB free before launch. Use the accepted Stage-1 supervisor, including completion/reaping before final budget decisions. Reserve each launch and retain every result. Sampling can detect a running-process limit only after small overshoot; report any observed overshoot.

A numerical refusal permits only the prescribed counterpart to run, then closes the later-input gate. A resource failure closes execution immediately. An unexplained trace/certificate mismatch closes the gate. Do not change inputs, retry again, loosen numerical checks or silently rerun. Preserve terminal problems for diagnosis. Any remedial experiment needs a new bounded prospective amendment within the authorized stage sequence.

## Completion and later scope

Successful completion means all six paired trajectories and unchanged checks pass within this declared envelope and independent audit has no unresolved findings. The result qualifies this 1,000-profile synthetic panel on the tested Mac; it does not establish unrestricted size, full-cohort, all-perturbation or R input qualification. This bounded scale milestone may advance to Stage 3 after audit. No larger-size step is included: use these measured costs to budget any future expansion. A negative outcome is reported and audited but does not permit dependent stages to begin.

Preserve primary evidence under the dated private stage02-scale directory, source here, and a new experiment record with a concise completion entry in Project status and next steps. Audit remains independent under the standing charter; handoff supplies facts and limitations only.
