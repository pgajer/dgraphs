% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radius_graphs.R
\name{cpp.create.rknn.graphs}
\alias{cpp.create.rknn.graphs}
\title{Compute Adaptive Radius-kNN Graphs With Batched ANN Search}
\usage{
cpp.create.rknn.graphs(X, kmin = NULL, kmax = NULL, ..., k.values = NULL)
}
\arguments{
\item{X}{Numeric matrix or data frame with observations in rows.}

\item{kmin, kmax}{Optional integer scalars defining the inclusive k range.
Required when \code{k.values} is \code{NULL}.}

\item{...}{Additional arguments forwarded to \code{create.rknn.graph()} with
\code{type = "adaptive.radius"}. The arguments \code{type}, \code{k.scale}, and \code{radius}
are reserved by this plural constructor.}

\item{k.values}{Optional integer vector of k values to evaluate. When
supplied, \code{k.values} is used instead of \code{kmin:kmax}, and the returned
graph order follows the supplied vector.}
}
\value{
A \code{"rknn_graphs"} object with the same default structure as
\code{create.rknn.graphs()}. When \code{return.timing = TRUE}, timing is attached at
the graph-sequence level because the ANN setup and max-k scale search are
shared across k values.
}
\description{
Tentative C++-backed counterpart to \code{create.rknn.graphs()}. It builds one
ANN kd-tree, computes nearest-neighbor distances through the maximal
requested k value once, derives each requested \code{k.scale} from that shared
result, and materializes adaptive-radius edge tables for all k values in
C++. The existing R finalization path is then used for pruning, lifecycle
branches, and optional component repair.

This function is intentionally named \code{cpp.create.rknn.graphs()} while the
native backend is being validated against \code{create.rknn.graphs()}. For
ordinary use, prefer \code{create.rknn.graphs()} until the backend selection API
is finalized.
}
\examples{
X <- matrix(c(0, 1, 3, 4), ncol = 1)
result <- cpp.create.rknn.graphs(
  X,
  k.values = c(1, 2),
  graph.detail = "minimal",
  prune.method = "none"
)
names(result$graphs)

}
\seealso{
\code{\link[=create.rknn.graphs]{create.rknn.graphs()}}
}
