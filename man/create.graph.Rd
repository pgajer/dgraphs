% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_graph.R
\name{create.graph}
\alias{create.graph}
\title{Create a Standard Graph}
\usage{
create.graph(type, n = NULL, ..., labels = NULL)
}
\arguments{
\item{type}{Graph type: \code{"empty"}, \code{"complete"}, \code{"chain"}, \code{"cycle"},
\code{"complete_bipartite"}, \code{"star"}, \code{"random"} or \code{"circle"}.}

\item{n}{Number of vertices. Required except when inferred from chain \code{x},
bipartite \code{sizes} or star \code{arms}. If supplied alongside those arguments,
it must agree. Empty graphs permit zero vertices; chains require at least
two, cycles and circles at least three, and other types at least one.}

\item{...}{Named type-specific arguments, described below. Unknown,
duplicated, unnamed and irrelevant arguments error.}

\item{labels}{Optional unique, nonmissing character or numeric vertex labels,
one per vertex. Labels name adjacency lists and are stored in
\code{metadata$labels}; they never change adjacency indices. For chains with
\code{x}, supply labels in the original input order.}
}
\value{
A \code{dgraph} with one \code{final} stage and construction information in
\code{metadata}, including \code{type}. Inspect it with \code{\link[=graph.adjacency]{graph.adjacency()}},
\code{\link[=graph.lengths]{graph.lengths()}}, \code{\link[=graph.edges]{graph.edges()}} and \code{\link[=graph.order]{graph.order()}}.
}
\description{
Construct a standard topology or a simple geometric graph. All types return
a \code{\link[=dgraph]{dgraph()}} and use compact, 1-based adjacency indices. \code{type} is required;
type names and names in \code{...} must match exactly.
}
\section{Types and arguments}{

\describe{
\item{\code{empty}, \code{complete}, \code{cycle}}{Only \code{n} and \code{labels}. These return
topology without stored lengths. A cycle closes a chain with one edge.}
\item{\code{chain}}{\code{span = 1} connects up to \code{span} positions on each side,
giving at most \code{2 * span} neighbors. It must be an integer from 1 to
\code{n - 1}; it does not use the nearest-neighbor meaning of \code{k}.
Optional finite numeric \code{x} orders vertices by coordinate (ties by input
position) and supplies absolute coordinate differences as lengths.
Without \code{x}, lengths are one. Optional vector \code{y} requires \code{x} and is
reordered with it. Metadata contains \code{order} (sorted-to-input indices),
\code{x.sorted} and \code{y.sorted}.}
\item{\code{complete_bipartite}}{Required \code{sizes}, two positive integers.
All pairs across the two parts are connected; there are no within-part
edges or stored lengths. Vertices in the first part come first.}
\item{\code{star}}{Required \code{arms}, at least two positive integer edge counts.
Chains of those lengths share vertex 1 as their center. Thus
\code{n = 1 + sum(arms)}; all-one arms give an ordinary star. Vertices are
numbered along successive arms. No lengths are stored.}
\item{\code{circle}}{\code{sampling = "uniform"} or \code{"random"} (default),
\code{edge.length = "arc"} (default) or \code{"chord"}, and optional \code{seed}.
Vertices are ordered by angle on the unit circle and consecutive
vertices, including last-to-first, are connected. Arc lengths measure
the shorter unit-circle arc between endpoints, even when the gap in
the sampling order exceeds pi. Chords are Euclidean distances.
Metadata contains \code{angles} (radians) and \code{coordinates} (x/y columns).}
\item{\code{random}}{Required finite \code{mean.degree}, \code{connected = TRUE}, and
optional \code{seed}. The edge count is \code{floor(n * mean.degree / 2)};
\verb{0 <= mean.degree <= n - 1}. If connected, this count must be at least
\code{n - 1}. A randomly ordered growing tree is built first, then a uniform
subset of remaining pairs is added. This is not uniform sampling over
connected graphs or spanning trees. If disconnected graphs are allowed,
a uniform subset of all pairs is sampled. Each undirected edge receives
one independent length uniform between 0.5 and 1.5. Candidate-pair storage is
quadratic in \code{n}; this constructor is intended for small examples.
Metadata records requested and realized mean degrees and connectivity.}
}
}

\section{Random numbers}{

For \code{random} and \code{circle}, \code{seed = NULL} uses and advances the caller's
current random stream. An explicit nonnegative integer seed makes the call
reproducible under the current RNG kind and restores the caller's RNG state
afterwards, including an initially absent state. Uniform circle sampling
consumes no random numbers. The richer geometry sampling API has its own
seed/state continuation contract; see \code{\link[=sample.synthetic.geometry]{sample.synthetic.geometry()}}.
}

\examples{
graph.order(create.graph("empty", n = 4))
graph.edges(create.graph("complete", n = 3))
chain <- create.graph("chain", x = c(3, 1, 2), span = 1,
                      labels = c("third", "first", "second"))
graph.adjacency(chain)
graph.lengths(chain)
graph.edges(create.graph("cycle", n = 4))
graph.order(create.graph("complete_bipartite", sizes = c(2, 3)))
graph.order(create.graph("star", arms = c(2, 3, 1)))
graph.edges(create.graph("random", n = 6, mean.degree = 2, seed = 10))
circle <- create.graph("circle", n = 8, sampling = "uniform",
                       edge.length = "chord")
circle$metadata$coordinates
}
