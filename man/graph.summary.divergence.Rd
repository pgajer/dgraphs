% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_summary_divergence.R
\name{graph.summary.divergence}
\alias{graph.summary.divergence}
\title{Compute divergence between two graphs using a selected graph summary}
\usage{
graph.summary.divergence(
  graph1,
  graph2,
  summary = c("degree_distribution", "edge_weight_distribution",
    "component_size_distribution", "neighborhood_label_distribution"),
  divergence = c("js"),
  labels = NULL,
  summary.args = list(),
  support = NULL,
  return.details = TRUE
)
}
\arguments{
\item{graph1}{First \code{dgraph} object.}

\item{graph2}{Second \code{dgraph} object.}

\item{summary}{Summary type to compare.}

\item{divergence}{Divergence type. Currently only \code{"js"}.}

\item{labels}{Optional shared vertex-label vector used for
\code{"neighborhood_label_distribution"}.}

\item{summary.args}{Optional named list forwarded to
\code{\link[=compute.graph.summary.pmf]{compute.graph.summary.pmf()}}.}

\item{support}{Optional common support. If \code{NULL}, the union of the two
PMF supports is used.}

\item{return.details}{Logical; if \code{TRUE}, return a structured list.}
}
\value{
If \code{return.details = TRUE}, a list with fields:
\itemize{
\item \code{summary}: summary type used.
\item \code{divergence}: divergence type used.
\item \code{value}: scalar divergence value.
\item \code{pmf1}: aligned PMF for \code{g1}.
\item \code{pmf2}: aligned PMF for \code{g2}.
\item \code{support}: aligned support.
\item \code{metadata}: summary-specific metadata for both graphs.
}

Otherwise, returns the scalar divergence value.
}
\description{
Extracts one selected graph summary from two graph-like objects and computes a
divergence between the resulting probability mass functions.

Currently the supported divergence is Jensen-Shannon divergence, implemented
via \code{\link[=jensen.shannon.divergence]{jensen.shannon.divergence()}}.
}
\examples{
path <- dgraph(list(2L, c(1L, 3L), 2L))
triangle <- dgraph(list(c(2L, 3L), c(1L, 3L), c(1L, 2L)))
graph.summary.divergence(
  path,
  triangle,
  summary = "degree_distribution"
)

}
