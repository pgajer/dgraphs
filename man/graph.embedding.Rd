% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_embedding_spectrum.R
\name{graph.embedding}
\alias{graph.embedding}
\title{Embed Graph in 2D or 3D Space}
\usage{
graph.embedding(
  graph,
  edge.attribute = NULL,
  transform = c("identity", "reciprocal"),
  dim = 2,
  method = c("fr", "kk"),
  verbose = FALSE,
  stage = "final"
)
}
\arguments{
\item{graph}{A \code{dgraph} object.}

\item{edge.attribute}{Explicit edge quantity, such as \code{"length"} or
\code{"conductance"}; \code{NULL} uses an unweighted layout.}

\item{transform}{Use selected values directly (\code{"identity"}) or their
reciprocals (\code{"reciprocal"}). Selected values must be strictly positive.
Fruchterman-Reingold treats larger values as stronger attraction, whereas
Kamada-Kawai uses them as distances.}

\item{dim}{Embedding dimension, either \code{2} or \code{3}.}

\item{method}{Layout method, \code{"fr"} or \code{"kk"}.}

\item{verbose}{Logical; print timing messages.}

\item{stage}{Stored graph stage used for both topology and edge attributes.}
}
\value{
Numeric layout matrix with one row per embedded vertex.
}
\description{
Embed Graph in 2D or 3D Space
}
\examples{
set.seed(1)
graph <- create.graph("cycle", 6)
graph.embedding(graph, dim = 2, method = "fr")

}
