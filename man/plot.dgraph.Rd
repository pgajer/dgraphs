% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dgraph.R
\name{plot.dgraph}
\alias{plot.dgraph}
\title{Plot a dgraph with Reusable Coordinates}
\usage{
\method{plot}{dgraph}(
  x,
  coordinates = NULL,
  vertex.values = NULL,
  vertex.colors = NULL,
  vertex.size = 1,
  edge.alpha = 0.2,
  color.palette = NULL,
  main = "",
  add.legend = TRUE,
  stage = "final",
  method = c("fr", "kk"),
  edge.attribute = NULL,
  transform = c("identity", "reciprocal"),
  projection = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{dgraph}.}

\item{coordinates}{Optional finite numeric matrix with one row per vertex
and two or three columns. Row order must match graph vertex order.}

\item{vertex.values}{Optional finite numeric value per vertex, mapped through
\code{color.palette}. Constant values use the palette midpoint and one legend key.}

\item{vertex.colors}{Optional explicit color or vector of colors, one per
vertex. Cannot accompany \code{vertex.values}. Defaults to \code{"steelblue"}.}

\item{vertex.size}{Nonnegative point size, one value or one per vertex.}

\item{edge.alpha}{Edge opacity between zero and one.}

\item{color.palette}{Color vector for numeric values; defaults to a
blue-white-red palette. Requires \code{vertex.values} when explicitly supplied.}

\item{main}{Plot title.}

\item{add.legend}{Logical; show value/color keys for numeric \code{vertex.values}.
Explicit colors have no automatically inferred numeric legend.}

\item{stage}{Stored graph stage, used for both layout and drawn edges.}

\item{method}{Layout method, \code{"fr"} or \code{"kk"}, when coordinates are absent.}

\item{edge.attribute, transform}{Explicit layout edge quantity and transform;
see \code{\link[=graph.embedding]{graph.embedding()}}. Layout arguments cannot accompany coordinates.}

\item{projection}{For three-column coordinates, two distinct column indices
to display, for example \code{c(1, 3)}. Three-dimensional input requires explicit
projection; it is never silently flattened. Not used for two-column input.}

\item{...}{Named arguments to \code{\link[graphics:plot.default]{graphics::plot.default()}}, such as \code{xlim},
\code{ylim}, \code{axes}, \code{xlab}, \code{ylab}, or \code{asp}. The arguments \code{x}, \code{y} and \code{type}
are controlled by this method and cannot be overridden.}
}
\value{
Invisibly returns the two-column coordinates actually drawn. These
can be supplied to subsequent calls to reuse the layout. Graphics settings
are restored after plotting.
}
\description{
Draw each undirected edge once and color vertices by values or explicit
colors. Coordinates are computed by \code{\link[=graph.embedding]{graph.embedding()}} only when absent.
Layout computation remains available separately in two or three dimensions.
}
\examples{
graph <- create.graph("cycle", n = 6)
xy <- plot(graph, vertex.values = 1:6)
plot(graph, coordinates = xy, vertex.values = rep(1, 6))
xyz <- graph.embedding(graph, dim = 3)
plot(graph, coordinates = xyz, projection = c(1, 3), vertex.colors = "navy")
}
