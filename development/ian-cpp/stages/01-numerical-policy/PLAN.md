# Stage 1: numerical policy qualification and typed integration

Authorized as part of the five-stage continuation on 23 September 2026. This plan precedes candidate code and fresh optimizer execution. Preserve all prior evidence and the strict numerical policy as an explicit baseline. This stage does not change a shared package installation or export an R function.

## Candidate

Integrate the independently accepted typed adapter source (d8f9d1b) into the coordinator's isolated implementation branch. Add an explicitly named candidate policy combining the Phase07E single normalized retry with Phase07K system-power constraint construction. Keep ordinary 1e-9 solver settings, retry 1e-11 settings, strict Solved status and unchanged 1e-7 external certificates. Retry only otherwise eligible Solved/AlmostSolved rejections, once per logical problem. Preserve original-unit validation and all raw attempts. No secondary objective, alternate backend or tolerance relaxation.

Represent policy, retry metadata and backend values with typed structures; serialize only at existing persistence boundaries. Validate input and restart policy declarations before execution; bind checkpoints to policy/configuration/source and physical attempt counts. Capture all 39 actual settings for every attempt and retain the existing layout guard. Preserve the default strict baseline during qualification. R may explicitly request the candidate; adoption follows audit, not construction.

## Prospective comparisons

Before full trajectories, compile without JSON numerical/event headers, exercise eligibility and malformed-return controls, and replay the six fixed difficult problems under ordinary and normalized settings. Compare fixed certificates independently. Run the preserved twelve boundary fixtures and six fixed decision controls through native and evaluated Python. Run the eight complete regression fixtures, four 500-profile geometries and frozen 1,000-profile helix in paired native/Python paths using the shared-power candidate. Compare every event, accepted/rejected optimization, pruning decision and final graph/scales/affinity under the frozen comparison rules. Retain prior saved controls as separately labeled baseline evidence.

Test strict-policy four-small-fixture preservation and explicit candidate R/native agreement. Test cancellation/resume after a natural successful retry, malformed/policy/source/counter restart refusal and observer failure. If any unexplained difference or new refusal occurs, stop dependent runs and preserve it; diagnose with a prospective amendment rather than changing policy in the running study.

## Bounds and evidence

Serial one-thread numerical work. Hard ceiling: 100 engine invocations plus 24 fixed LP solves, 8,000 total physical attempts, 1,000 per engine call, 3,600 aggregate child seconds, 900 seconds per process, 4 GiB process-tree resident memory, 24 GiB study outputs and at least 20 GiB free space. Reserve calls before launch, reap completion before evaluating final-budget closure, and retain failed launches/accounting. No numerical smoke calls outside the ledger. Read-only validation and compilation are separate from optimization.

Freeze fixture identities and exact schedule before calls; commit harness/source before measured execution. Generated evidence and factual handoffs belong under /Users/pgajer/.codex/private/ZB/ian-cpp/2026-09-23/stage01-policy/. Source remains in the assigned implementation worktree. Any additional diagnostic solves require a recorded bounded amendment before launch and remain in total accounting.

## Completion

Complete the frozen schedule or document the exact failure gate. Independent review is by IAN auditor under the Audit Charter; handoffs give facts and limitations, not audit instructions. Resolve every finding before advancement. A passing bounded candidate may be adopted for the maintained internal implementation within this owner's sequence authorization, with exact version and limitations recorded. A negative study remains informative but does not release the scale gate. Cross-platform arithmetic evidence belongs to stage 3 and is not implied by local success.
