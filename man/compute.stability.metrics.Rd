% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iknn_selection.R
\name{compute.stability.metrics}
\alias{compute.stability.metrics}
\title{Compute Stability Metrics Across a Sequence of IkNN Graphs}
\usage{
compute.stability.metrics(
  graphs,
  graph.type = c("geom", "isize"),
  summary = c("degree_distribution", "edge_weight_distribution",
    "component_size_distribution", "neighborhood_label_distribution"),
  divergence = c("js"),
  labels = NULL,
  summary.args = list()
)
}
\arguments{
\item{graphs}{An object of class \code{"iknn_graphs"} returned by
\code{\link[=create.iknn.graphs]{create.iknn.graphs()}}.}

\item{graph.type}{Character string, either \code{"geom"} or \code{"isize"}.}

\item{summary}{Graph-summary family used for the divergence curve.}

\item{divergence}{Divergence used for consecutive-graph comparison.
Currently only \code{"js"}.}

\item{labels}{Optional vertex-label vector used when
\code{summary = "neighborhood_label_distribution"}.}

\item{summary.args}{Optional named list forwarded to
\code{\link[=compute.graph.summary.stability]{compute.graph.summary.stability()}}.}
}
\value{
An object of class \code{"iknn_stability_metrics"}.
}
\description{
Computes stability diagnostics across the pruned IkNN graphs produced by
\code{\link[=create.iknn.graphs]{create.iknn.graphs()}}. Metrics include edit distances between consecutive
graphs, graph-summary Jensen-Shannon divergence, edge-count diagnostics,
local minima, and smoothed trend fits for plotting.
}
\examples{
set.seed(1)
x <- matrix(rnorm(60), ncol = 2)
graphs <- create.iknn.graphs(
  x, kmin = 2, kmax = 4, compute.full = TRUE,
  n.cores = 1, verbose = FALSE
)
stability <- compute.stability.metrics(graphs)
stability$edit.distances
}
