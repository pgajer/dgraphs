# Stage 3 packaging correction and final verification

The first fresh installations/backend builds and all 52 planned R entries passed (414 attempts). Full R-devel check finished with zero errors/warnings and six notes: development version/private suggested package, hidden upstream .cargo, long vendor paths, upstream CITATION location, optional dynamic native-call expression, and unavailable recent HTML Tidy. Preserve the entire first archive, build, numerical and check evidence.

Correct the IAN distribution notes by shipping the unchanged backend source tree as a deterministic source ZIP inside the package; keep the editable tree in the repository. The existing package build helper creates the ZIP before R builds and removes that generated input afterward. The installed backend helper verifies/expands it in the fresh build directory. Keep license notices with the sources, an accessible source/identity manifest, and no new runtime dependency. The core and solver source bytes remain unchanged. This avoids R treating upstream project metadata as package metadata and removes long paths from the outer R archive. Do not remove upstream required build inputs or rewrite numerical code.

Resolve the foreign-call checker note by assigning the already resolved native address to a local variable before .Call. Its arguments and symbol resolution stay unchanged. Document actual accepted scope and installed-source access. Use the existing recent Homebrew HTML Tidy for the final package checks. The development version and optional ivue repository note remain explicitly outside CRAN-release readiness; neither changes internal functionality.

Create a separately versioned corrected source archive, fresh private installations and fresh installed-source backend builds in both R runtimes. Final numerical verification uses the twelve remaining authorized engine entries: per runtime, full strict PreSSMat trace, full explicit-policy 500-profile helix, three explicit-policy refusal controls and one relocated-module case. Compare unchanged outputs and traces against the corresponding first-version evidence and accepted references. All twelve share the original cumulative 64-entry / 2,000-attempt ceiling; no additional numerical capacity is authorized by this amendment. The other first-version comparisons remain identified as earlier evidence for the same unchanged numerical core.

Run full package checks against the corrected archive in both named runtimes, retaining notes rather than treating exit status alone as a clean result. Read-only archive/source/AST and diagnostic checks do not invoke the engine. No Stage 4 work begins until independent Stage 3 acceptance and closure of every finding.
