% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodesic_neighbors.R
\name{geodesic.knnx}
\alias{geodesic.knnx}
\title{Find Geodesic Neighbors from Grid Queries to Observations}
\usage{
geodesic.knnx(
  X,
  X.grid,
  k,
  k.graph = 5,
  small.component = c("error", "truncate")
)
}
\arguments{
\item{X}{Finite numeric matrix of data observations.}

\item{X.grid}{Finite numeric matrix of grid query points, with the same
number of coordinate columns as \code{X}.}

\item{k}{Number of data observations returned per grid query, in \code{1:nrow(X)}.
Grid and data rows have distinct identities: a coincident observation is
a valid zero-distance neighbor. Ties are ordered by data row index.}

\item{k.graph}{Number of grid vertices attached to each observation, in
\code{1:nrow(X.grid)}. Grid vertices connect to up to twice the coordinate
dimension other grid vertices. No components are automatically bridged.}

\item{small.component}{Error on insufficient reachable observations, or
explicitly \code{"truncate"} and pad absent indices and distances with \code{NA}.}
}
\value{
A list with combined coordinates \code{V} (grid rows first), edge pairs
\code{E} in V row indices, \code{nn.index} (indices into X), \code{nn.dist} and
\code{effective.k} (the number returned per grid query).
}
\description{
Find Geodesic Neighbors from Grid Queries to Observations
}
\examples{
X.grid <- as.matrix(expand.grid(x = 0:2, y = 0:2))
X <- rbind(c(0, 0), c(1.2, 0.8), c(1.8, 1.7))
geodesic.knnx(X, X.grid, k = 2)
}
\seealso{
\code{\link[=geodesic.knn]{geodesic.knn()}}
}
