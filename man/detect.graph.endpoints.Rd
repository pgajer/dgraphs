% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_endpoint_geometry.R
\name{detect.graph.endpoints}
\alias{detect.graph.endpoints}
\title{Detect Graph Endpoints from a 3D Embedding}
\usage{
detect.graph.endpoints(
  adj.list,
  weight.list,
  layout.3d,
  neighborhood = c("geodesic_k", "geodesic_radius"),
  k = c(10L, 20L, 30L),
  radius = NULL,
  q = 0.1,
  neighbor.weighting = c("uniform", "inverse_distance", "gaussian"),
  gaussian.sigma = NULL,
  min.neighborhood.size = 3L,
  scale.aggregate = c("mean", "median", "min", "max"),
  score.metric = c("score", "m", "s.q", "s.min"),
  smooth = FALSE,
  fitted.model = NULL,
  smooth.fit.args = NULL,
  smooth.refit.args = NULL,
  detect.max.radius = 2,
  detect.min.neighborhood.size = 2L,
  scale.stability.radius = NULL,
  min.scale.stability = 0.5,
  min.score.quantile = 0.8,
  max.endpoints = NULL,
  verbose = FALSE
)
}
\arguments{
\item{adj.list}{Graph adjacency list (1-based vertex indices).}

\item{weight.list}{Edge-length list aligned with \code{adj.list}.}

\item{layout.3d}{Numeric matrix or data frame with 3 columns giving the 3D
embedding coordinates for graph vertices.}

\item{neighborhood}{Character string. Either \code{"geodesic_k"} (default) or
\code{"geodesic_radius"}.}

\item{k}{Integer vector of geodesic neighborhood sizes used when
\code{neighborhood = "geodesic_k"}.}

\item{radius}{Positive numeric vector of geodesic radii used when
\code{neighborhood = "geodesic_radius"}.}

\item{q}{Quantile used for \code{s.q}. Must lie in \verb{[0, 1]}. Default is \code{0.1}.}

\item{neighbor.weighting}{Character string controlling neighbor weights for
\code{m} and weighted pair quantiles. One of \code{"uniform"} (default),
\code{"inverse_distance"}, or \code{"gaussian"}.}

\item{gaussian.sigma}{Optional positive numeric scale used when
\code{neighbor.weighting = "gaussian"}.}

\item{min.neighborhood.size}{Minimum number of usable neighbors required after
removing zero-length embedding vectors.}

\item{scale.aggregate}{How to aggregate scores across multiple neighborhood
scales. One of \code{"mean"} (default), \code{"median"}, \code{"min"}, or \code{"max"}.}

\item{score.metric}{Which score to maximize. One of \code{"score"} (default),
\code{"m"}, \code{"s.q"}, or \code{"s.min"}.}

\item{smooth}{Logical; if \code{TRUE}, smooths aggregated scores on the graph
before detecting extrema.}

\item{fitted.model}{Optional fitted graph smoother from
\code{fit.rdgraph.regression()}. If supplied, smoothing reuses its spectral
decomposition via \code{refit.rdgraph.regression()}.}

\item{smooth.fit.args}{Optional named list of arguments passed to
\code{fit.rdgraph.regression()} when \code{smooth = TRUE} and \code{fitted.model} is
\code{NULL}. \code{X}, \code{y}, \code{adj.list}, \code{weight.list}, and \code{verbose.level} are filled
automatically when not supplied.}

\item{smooth.refit.args}{Optional named list of arguments passed to
\code{refit.rdgraph.regression()} when smoothing aggregated score fields.}

\item{detect.max.radius}{Maximum radius passed to \code{\link[=detect.local.extrema]{detect.local.extrema()}}
when calling endpoint candidates from the final detection score.}

\item{detect.min.neighborhood.size}{Minimum neighborhood size passed to
\code{\link[=detect.local.extrema]{detect.local.extrema()}} when calling endpoint candidates.}

\item{scale.stability.radius}{Optional non-negative geodesic radius used when
translating per-scale local maxima into multiscale support. If \code{NULL}
(default), uses \code{detect.max.radius}. This allows smoothed maxima that shift
slightly inward to remain supported by nearby raw multiscale maxima.}

\item{min.scale.stability}{Minimum fraction of scales for which a vertex must
be a local maximum of the per-scale detection score to be retained as an
endpoint. Default is \code{0.5}.}

\item{min.score.quantile}{Quantile filter applied to the final detection
score before returning endpoints. Default is \code{0.8}, which suppresses weak
local maxima while keeping high-scoring terminal tips.}

\item{max.endpoints}{Optional positive integer cap on returned endpoints.}

\item{verbose}{Logical; if \code{TRUE}, prints progress.}
}
\value{
A list with endpoint vertices, score diagnostics, optional smoothed
scores, scale stability summaries, and the local-extrema fit used for
endpoint calling.
}
\description{
Detects terminal arm tips in a graph by:
\enumerate{
\item computing graph endpoint scores from the 3D embedding,
\item optionally smoothing those scores on the graph using
\code{fit.rdgraph.regression()} and \code{refit.rdgraph.regression()},
\item finding graph-local maxima with \code{\link[=detect.local.extrema]{detect.local.extrema()}},
\item retaining only maxima that persist across neighborhood scales.
}

The function works with terminal regions in an embedding; returned endpoints
need not be degree-1 vertices.
}
