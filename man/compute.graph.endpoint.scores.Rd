% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_endpoint_geometry.R
\name{compute.graph.endpoint.scores}
\alias{compute.graph.endpoint.scores}
\title{Compute Graph Endpoint Scores from a 3D Embedding}
\usage{
compute.graph.endpoint.scores(
  adj.list,
  weight.list,
  layout.3d,
  neighborhood = c("geodesic_k", "geodesic_radius"),
  k = c(10L, 20L, 30L),
  radius = NULL,
  q = 0.1,
  neighbor.weighting = c("uniform", "inverse_distance", "gaussian"),
  gaussian.sigma = NULL,
  min.neighborhood.size = 3L,
  scale.aggregate = c("mean", "median", "min", "max"),
  verbose = FALSE
)
}
\arguments{
\item{adj.list}{Graph adjacency list (1-based vertex indices).}

\item{weight.list}{Edge-length list aligned with \code{adj.list}.}

\item{layout.3d}{Numeric matrix or data frame with 3 columns giving the 3D
embedding coordinates for graph vertices.}

\item{neighborhood}{Character string. Either \code{"geodesic_k"} (default) or
\code{"geodesic_radius"}.}

\item{k}{Integer vector of geodesic neighborhood sizes used when
\code{neighborhood = "geodesic_k"}.}

\item{radius}{Positive numeric vector of geodesic radii used when
\code{neighborhood = "geodesic_radius"}.}

\item{q}{Quantile used for \code{s.q}. Must lie in \verb{[0, 1]}. Default is \code{0.1}.}

\item{neighbor.weighting}{Character string controlling neighbor weights for
\code{m} and weighted pair quantiles. One of \code{"uniform"} (default),
\code{"inverse_distance"}, or \code{"gaussian"}.}

\item{gaussian.sigma}{Optional positive numeric scale used when
\code{neighbor.weighting = "gaussian"}. If \code{NULL}, the median positive geodesic
neighborhood distance is used per vertex and scale.}

\item{min.neighborhood.size}{Minimum number of usable neighbors required after
removing zero-length embedding vectors. Must be at least \code{2}.}

\item{scale.aggregate}{How to aggregate scores across multiple neighborhood
scales. One of \code{"mean"} (default), \code{"median"}, \code{"min"}, or \code{"max"}.}

\item{verbose}{Logical; if \code{TRUE}, prints progress.}
}
\value{
A list with aggregated per-vertex scores, per-scale score matrices,
and neighborhood diagnostics.
}
\description{
Computes endpointness diagnostics for graph vertices by selecting graph-geodesic
neighborhoods and evaluating how concentrated the corresponding 3D embedding
directions are. This targets terminal arm tips in an embedding rather than
graph-theoretic degree-1 vertices.

For each vertex \eqn{x}, let \eqn{u_1, \dots, u_m} be unit vectors in the
embedding from \eqn{x} to graph-geodesic neighbors of \eqn{x}. The function
computes:
\deqn{s_{\min}(x) = \min_{i < j} u_i^\top u_j}
\deqn{s_q(x) = \mathrm{quantile}\{u_i^\top u_j : i < j\}}
\deqn{m(x) = \left\| \frac{1}{\sum_i w_i} \sum_i w_i u_i \right\|}
\deqn{\mathrm{score}(x) = m(x)\, \frac{1 + s_q(x)}{2}}

Neighborhoods are selected in graph-geodesic space while directions are taken
from the supplied 3D embedding.
}
