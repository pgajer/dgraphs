# Stage 1 continuation after reference packaging failure

The first schedule completed all 12 fixed LP replays with expected certificates. Native six-case decision controls completed without optimization. Python's corresponding launch failed before any solver because the derived reference folder omitted config.json. A no-solve eligibility test initially failed to compile due a duplicated namespace; its corrected 86-case native/Python truth table passed.

Preserve build-v1, qualification-v1 and eligibility-v1 as evidence. Copy the missing configuration into a separately identified reference-v2 directory, with the candidate policy named consistently, and correct the derivation recipe. Resume with the decision/boundary/full panel, retaining all prior calls and 12 optimizer attempts in the same ceilings. Do not repeat the fixed LP solves. Repeating the two zero-optimization decision-control launches is permitted. No numerical implementation or acceptance rule changes.
