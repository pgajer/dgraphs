% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodesic_neighbors.R
\name{geodesic.knn}
\alias{geodesic.knn}
\title{Find Geodesic Neighbors Within a Point Cloud}
\usage{
geodesic.knn(
  X,
  k,
  k.graph = 5,
  graph = NULL,
  small.component = c("error", "truncate")
)
}
\arguments{
\item{X}{Finite numeric matrix of observations, one per row.}

\item{k}{Number of other vertices to return, an integer in \code{1:(nrow(X)-1)}.
Self is excluded by vertex index; coincident other observations remain
valid zero-distance neighbors. Ties are ordered by vertex index.}

\item{k.graph}{Number of other neighbors used to construct the auxiliary
symmetric kNN graph. Components are not automatically connected.
Ignored when \code{graph} is supplied.}

\item{graph}{Optional \code{dgraph} containing stored edge lengths, in X row order.}

\item{small.component}{Error if fewer than \code{k} other vertices are reachable
(\code{"error"}, default), or return the available neighbors (\code{"truncate"}).}
}
\value{
A list with \code{nn.index} and \code{nn.dist} matrices with \code{k} columns and
\code{effective.k}, the number returned per row. Under explicit truncation,
absent indices and distances are \code{NA}; unreachable vertices are never
reported as neighbors.
}
\description{
Find Geodesic Neighbors Within a Point Cloud
}
\examples{
X <- cbind(0:3, 0)
geodesic.knn(X, k = 1, k.graph = 1) # one other vertex per row
}
\seealso{
\code{\link[=geodesic.knnx]{geodesic.knnx()}}, \code{\link[=graph.geodesic.distances]{graph.geodesic.distances()}}
}
