% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ian_graph.R
\name{create.ian.graph}
\alias{create.ian.graph}
\title{Experimental Internal IAN Adapter}
\usage{
create.ian.graph(
  X,
  distances = NULL,
  specimen.ids = NULL,
  participant.ids = NULL,
  graph = NULL,
  diagnostics = c("summary", "full"),
  backend = NULL,
  max.solves = 10000L,
  numerical.policy = "IAN evaluated-LP 1.0"
)
}
\arguments{
\item{X}{Finite numeric specimen-by-feature matrix, at least one column.}

\item{distances}{Optional unsquared distance matrix or \code{dist}. When absent,
Euclidean distances are computed with \code{stats::dist(X)}. Supplied distances
are used exactly; duplicate feature rows must have identical distance rows.
Coordinates are always required to define duplicate profiles. Row/column
order must match X; named distances must match specimen IDs.}

\item{specimen.ids}{Unique nonempty IDs; defaults to row names or row numbers.}

\item{participant.ids}{Optional specimen-level nonempty IDs, possibly repeated.}

\item{graph}{Reserved supplied-graph argument; currently must be \code{NULL}.
The actual initial Gabriel graph is always constructed by IAN.}

\item{diagnostics}{\code{"summary"} or \code{"full"}; full includes dense LP payloads.}

\item{backend}{Optional path to the separately built \code{dgraphs_ian.so} module.}

\item{max.solves}{Positive integer safety limit; reaching it returns refusal.}

\item{numerical.policy}{Exactly \code{"IAN evaluated-LP 1.0"} (strict default) or
\code{"IAN evaluated-LP retry-power 0.1"} (explicit experimental candidate).}
}
\value{
A list with \code{complete}, structured \code{error}, initial Gabriel graph,
\code{final_graph} only on full completion, \code{last_valid_graph} (possibly partial),
profile mapping, local scales and an affinity matrix distinct from metric
edge lengths. Graph indices and mappings are one-based. Full traces retain
original zero-based core indices and declare this in diagnostics. Summary diagnostics
include per-solve status, acceptance, settings, residuals and iteration counts.
Refusal and interruption return incomplete structured results; invalid R
arguments error before native execution. Interrupts are checked at engine
events; they do not interrupt an active solver call.
}
\description{
The unexported adapter requires the separately built optional backend. Its
unchanged default, \verb{IAN evaluated-LP 1.0}, uses strict numerical acceptance
without retries. The explicit experimental option
\verb{IAN evaluated-LP retry-power 0.1} uses shared-power constraint arithmetic and
at most one normalized retry after an eligible rejected return. The retry must
return strict solver success and pass the unchanged original-unit checks; the
original rejected result is never accepted directly. This candidate has not
been adopted as the default. It has independent bounded qualification on
macOS arm64 and is selected explicitly for internal qualification studies.
See \code{system.file("ian", "README.md", package = "dgraphs")} for build and
numerical limitations. Input rows are specimens; graph vertices are unique
feature profiles in first-occurrence order.
}
\keyword{internal}
