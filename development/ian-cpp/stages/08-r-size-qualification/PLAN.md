# Remove the arbitrary R row cap and qualify larger runs

23 September 2026. The owner authorizes removing the R adapter's fixed 500-row rejection, retaining meaningful input/index checks and solver/interrupt controls, documenting resource scaling, qualifying the seven frozen 1,000-profile geometries including connected mode, then bounded larger tests and independent audit. This is IAN-EXP-034. No new public export, default, numerical rule, shared installation or scientific claim is included.

## Interface and representation

Permit any specimen-row count representable by the implemented data structures and available resources. Do not replace 500 with another experimental row threshold. Retain the minimum of two rows, all existing input checks, the solve budget and event-boundary interrupt behavior. Before distance creation and native conversion, check matrix dimensions/product against actual R vector/index representations and the core's signed-index arithmetic. Check C++ to R projection dimensions and integral conversions before narrowing or allocation. Exercise these boundaries with dimension-only helpers so no oversized arrays are allocated. These technical representation limits differ from experimental memory/time ceilings.

Document quadratic dense-matrix storage, multiple simultaneous matrices/copies, cubic worst-case Gabriel construction and extra full-trace storage. One n by n double matrix occupies 8*n*n bytes, excluding overhead. This is a component-size calculation, not a peak-memory estimate or a promise that a run fits. Keep summary diagnostics as default. R arguments and newly introduced R helpers use dotted names. Preserve existing return shape, pruning/numerical defaults and complete/refusal semantics.

## Qualification sequence

Commit source and fixtures before measured runs. Freshly build the optional native module/CLI from a pinned source snapshot and freshly install the source archive privately on the available Mac arm64 R-devel runtime. Reuse the identified accepted Rust archive; do not claim a new Rust/platform build. Regenerate help, run targeted tests and the full archive check.

First execute all seven existing 1,000-profile geometries (helix, cloud, lobes, intrinsic 2–5 dimensional quadforms) in reference and connected pruning modes, natively and through R: 28 entries. Preserve original coordinates/distances, changing only the explicit connectivity flag. All select the accepted retry-power numerical policy. Full traces allow native/R equality, accepted/rejected-return certificate checks, independent bridge/current-component checks, final graph/scale/affinity and R mapping/history checks. Reference native traces must reproduce the accepted earlier reference runs. Compare complete outputs or matching refusals; do not change a numerical limit to obtain completion.

Four strict-policy small R controls and selected saved 200-point connected cases test behavior below the former boundary. R operational controls exercise more than 500 specimen rows, duplicate mapping, summary/full parity, solve-budget refusal and early interruption. Small/fixed dimension-only controls check index arithmetic and errors before backend execution. An absent optional module on a valid 501/1,000-row input must produce its existing unavailable-module error, not a row-cap error.

If the 1,000-profile panel completes with no unexplained discrepancy or resource stop, run two prescribed 2,000-point engineering samples: a helix generated with PCG64 SeedSequence([1, 8201]), uniform parameter on [0, 4*pi], coordinates (cos(t), sin(t), .15*t); and a four-dimensional quadform generated with PCG64(8202), latent-uniform [-1,1]^4, diagonal form (+.25,-.25,+.25,-.25), embedding (u,u^T A u). Use scalar distance checks, freeze before execution, and compare connected native/full against R/summary exact final objects and projected histories. These are engineering samples, not scientific or dgraphs-generator qualification. No R full trace is required at this size.

A single 5,000-point connected helix pair, with generator seed 8203, is permitted only if both 2,000-point pairs complete, each stays below 60 child seconds and 1 GiB sampled tree RSS, and at least 20 GiB disk remains. Otherwise record the larger step as unexecuted by its prospective gate; do not resample. The 5,000-point pair uses summary observation, and exact result/history agreement with its own native summary client; it does not replace full certificate evidence at 1,000/2,000. A resource stop closes larger execution immediately. A numerical refusal permits the paired counterpart and then closes the later-size gate. Implementation/interface discrepancies require diagnosis with separately versioned bounded evidence. There is no requirement to reach 5,000.

## Accounting and completion

Maximum 64 attempted engine entries, 16,000 physical solver attempts, 1,500 attempts/entry, 900 seconds/numerical process, 7,200 aggregate child seconds, 4 GiB sampled tree RSS, 2 GiB output/process and 24 GiB study evidence. Serial numerical launches, one numerical thread, reservations before launch, complete reaping and preserved refusals. Build/package commands have separate logs and time bounds. Existing guard may detect a resource limit after small sampling overshoot; retain and report it. Author checkers and input generation have separate command records and zero optimizer calls.

The change can remove an artificial execution restriction without claiming all larger inputs complete. Report the permitted behavior, actual tested sizes/modes, refusals and unexecuted steps separately. Completion includes a maintained catalogue/status record, useful R documentation, exact audit handoff and independent disposition with all findings resolved. Preserve earlier accepted reports and original failed evidence. No cohort runs or biological/predictive acceptance is implied.
