# Phase 06A: reusable core, unchanged numerical policy

Authorized by the user's instruction to start Phase 06A. Base: `68eded5`.
Phase05 accepted candidate: `b2e132562ae61a8849d2c1b8bca317fb805f76b1`.
The coordinator/implementer performs this bounded work; independent acceptance
remains a separate decision. Sources are committed before builds and executions.

Extract a C++17 library with typed in-memory input, mapping, graph, result and
error interfaces. A fresh call owns its state; no output directory, JSON file,
Python process or R runtime is required by the core. Keep JSON trace formatting
as an optional diagnostic observer and existing stage files in a CLI adapter.
The CLI preserves legacy trace fields and checkpoint behavior. Stable typed
results expose partial stage availability and structured errors. Optional
participant identities are supplied per specimen and never inferred from feature
duplicates. Document schema versions, indexing, ownership, units, affinities,+support/diagonal policy and the scientific consumer boundary.

Keep the accepted arithmetic, ordering, solver assembly/settings, caps, duplicate
policy and isolate policy unchanged. Copy pinned numerical sources into private
library implementation files, with attribution and mechanically checked bodies.
Keep old phase sources/evidence and all auditor directories immutable. Version
new result schemas without changing the numerical reference IAN evaluated-LP 1.0.
Retain original-expression historical failures as accepted limitations.

## Frozen validation workload

1. Run the new CLI once on each of the four accepted phase03 ordinary fixtures
   and four phase05 complete fixtures (eight complete runs). Compare every event
   to saved accepted native traces, excluding elapsed time, and apply the frozen
   phase03 numerical/discrete comparator. Compare against saved evaluated-Python
   traces as well. Recompute every new raw optimization payload and checkpoint.
2. Run all twelve frozen phase05 fixed-state probes and its six threshold/tie
   vectors through the extracted core. Compare to accepted native and evaluated
   Python evidence. No new calibration or input search.
3. Retain phase03's explicit Gabriel, duplicate, disconnected-state and affinity
   cutoff stage tests, four controlled exceptions and the pruning-cap test.
   These are regressions of existing semantics, not crash or resume tests.
4. Test the typed API directly, without an observer/filesystem, on the public
   phase03 nonuniform-curve example twice in one process; compare result values
   with its CLI result. Test supplied participant mappings, exact duplicates,
   invalid dimensions/nonfinite values, unsupported schema/policy, malformed
   participant mappings, and observer exceptions. No new numerical settings.
5. Build in a separate clean prefix/home/environment using source snapshots and
   pinned vendored Rust crates. Rebuild Clarabel with the same Rust toolchain,
   lockfile and features rather than reuse the development solver binary.
   Compiler/SDK tools may be shared; no production environment is modified.
   Install only in the private prefix, compile an external C++ consumer using
   installed public headers/targets, and rerun the public helix fixture once.
6. Build a minimal local R .Call bridge to the installed typed core, without
   Rcpp or a package installation. Run the same public helix once and compare
   all exposed graph, mapping, scale and affinity outputs with the CLI. Verify
   indexing, invalid input handling and ownership; record R/toolchain versions
   and copies. This is one-host feasibility, not platform/package qualification.

Bound: at most 700 new optimization payloads for the planned workload, including
diagnostics, excluding a separately recorded correction rerun if a defect is
found. No cohort input expansion, Python solver sweep or original-control rerun.
Stop the driver at an unexpected comparison failure; preserve and diagnose it,
commit any correction before a new output namespace, and never relax tolerances.
Successful prior jobs may be reused only with verified hashes and source identity.

## Completion and limits

Record all attempts and failures, core/API validation, clean-build and R outcomes,
source/dependency identities, numerical comparisons, and a factual handoff.
Completion of the bounded work is separate from independent acceptance or
authorization for further phases. Freeze the submission and stop for audit.

No checkpoint/resume mechanism, cancellation implementation, concurrency promise,
new solver backend, algorithm improvement, larger-cohort run, performance claim,
public release or shared-library installation is part of Phase06A. Preserve the
diagnostic warning evidence; a root-cause investigation is tracked separately.
No new agents, tasks, messages to others or automations are included.
