% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ian_backend.R
\name{build.ian.backend}
\alias{build.ian.backend}
\title{Build the Optional IAN Backend}
\usage{
build.ian.backend(
  build.dir,
  python = "python3",
  cargo = "cargo",
  rustc = "rustc",
  cxx = "clang++",
  offline = FALSE
)
}
\arguments{
\item{build.dir}{New output directory, including logs and source copies.
Existing files/directories are refused. A path containing spaces is allowed.}

\item{python}{Python 3 command or executable path.}

\item{cargo}{Cargo command or executable path from a native arm64 toolchain.}

\item{rustc}{Rust compiler command or path from the same toolchain as \code{cargo}.}

\item{cxx}{C++ compiler command or executable path.}

\item{offline}{Logical scalar. If \code{TRUE}, Cargo uses only cached dependencies.}
}
\value{
The absolute path to \code{dgraphs_ian.so}, invisibly. Save this path and pass
it to \code{create.ian.graph(..., backend = path)}. Reuse the built module with the
same R installation; rebuild when changing R runtime or backend sources.
Build errors retain available logs and raise an R error. No shared package
library is modified. Build records identify source files and tool commands.
}
\description{
Compile the pinned IAN and Clarabel sources installed with dgraphs in a new
directory. The supported build target is macOS arm64. Setup requires Python 3,
Apple's C++ compiler/SDK, and matching native Cargo/Rust tools (Rust >= 1.77).
An initial build needs cached crates or network access. No tools are installed
by this function. Compilation is explicit and never starts inside
\code{\link[=create.ian.graph]{create.ian.graph()}} or ordinary package installation.
}
\examples{
\dontrun{
backend <- build.ian.backend("ian-build")
fit <- create.ian.graph(matrix(c(0, 1, 2), ncol = 1), backend = backend)
stopifnot(fit$complete)
}
}
\seealso{
\code{\link[=create.ian.graph]{create.ian.graph()}}
}
